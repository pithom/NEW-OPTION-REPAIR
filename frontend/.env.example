# For local development, `/api` works with the Vite proxy in `vite.config.js`.
# For a separately hosted frontend, set this to your backend origin, for example:
# VITE_API_URL=https://newoption-repair-backend.onrender.com
VITE_API_URL=/api
