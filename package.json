{
  "name": "new-option-technology",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "start /b cmd /c \"npm run dev:backend\" && npm run dev:frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "reset:admin": "npm run reset:admin --prefix backend",
    "render:build": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  }
}
